const MuiDrawer = {
    defaultProps: {
        PaperProps: {
            sx: {
                backgroundColor: '#fff',
                boxShadow: 'unset',
                borderRight: 'unset'
            }
        },
    }
};

export default MuiDrawer;
