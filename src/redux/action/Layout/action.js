import {SWITCH_DRAWER_MODE, SWITCH_DRAWER_NARROW_MODE} from "./actionType";

export const switchDrawerMode = () => ({
    type: SWITCH_DRAWER_MODE,
});

export const switchDrawerNarrowMode = () => ({
    type: SWITCH_DRAWER_NARROW_MODE,
});
